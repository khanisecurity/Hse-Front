@inherits LayoutComponentBase

@code {
    protected bool isLoading = true;

    protected async Task LoadDataAsync(Func<Task> loadMethod)
    {
        isLoading = true;
        await loadMethod();
        isLoading = false;
    }
}